#include "Sales_data.h"

int main() {
    Sales_data lhs, rhs;
    read(read(cin, lhs), rhs);
    print(print(cout, lhs) << endl, rhs) << endl;
    Sales_data sum = add(lhs, rhs);
    print(cout, sum);
}